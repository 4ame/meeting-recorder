# Toggle génération CR — Plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ajouter un toggle "Générer le CR" dans le menu tray, persisté dans `~/.meeting_recorder/settings.json`, désactivé par défaut.

**Architecture:** Nouveau module `src/config.py` (lecture/écriture JSON) isolé et testable. `tray.py` consomme ce module pour initialiser l'état au démarrage, exposer le toggle dans le menu, et conditionner l'appel Gemini. `process.py` n'est pas modifié.

**Tech Stack:** Python stdlib (`json`, `pathlib`), `pystray` (checkmark natif via `checked=`), `pytest` + `unittest.mock`.

---

## Fichiers concernés

| Action  | Fichier                            | Rôle                                              |
|---------|------------------------------------|---------------------------------------------------|
| Créer   | `src/config.py`                    | Persistance `~/.meeting_recorder/settings.json`   |
| Créer   | `tests/test_config.py`             | Tests unitaires de `config.py`                    |
| Modifier | `src/tray.py`                     | Import, global, toggle, menu, `process_async`     |

---

## Task 1 : `src/config.py` — module de persistance

**Files:**
- Create: `src/config.py`
- Create: `tests/test_config.py`

- [ ] **Step 1 : Écrire les tests**

Créer `tests/test_config.py` :

```python
import json
import pytest
from pathlib import Path
from unittest.mock import patch
import config as cfg


def test_load_settings_returns_defaults_when_file_missing(tmp_path):
    with patch.object(cfg, "_CONFIG_FILE", tmp_path / "nonexistent.json"):
        result = cfg.load_settings()
    assert result == {"cr_enabled": False}


def test_load_settings_reads_existing_file(tmp_path):
    config_file = tmp_path / "settings.json"
    config_file.write_text('{"cr_enabled": true}', encoding="utf-8")
    with patch.object(cfg, "_CONFIG_FILE", config_file):
        result = cfg.load_settings()
    assert result["cr_enabled"] is True


def test_load_settings_returns_defaults_on_corrupt_json(tmp_path):
    config_file = tmp_path / "settings.json"
    config_file.write_text("not valid json", encoding="utf-8")
    with patch.object(cfg, "_CONFIG_FILE", config_file):
        result = cfg.load_settings()
    assert result == {"cr_enabled": False}


def test_save_settings_writes_file(tmp_path):
    config_file = tmp_path / "settings.json"
    with patch.object(cfg, "_CONFIG_FILE", config_file), \
         patch.object(cfg, "_CONFIG_DIR", tmp_path):
        cfg.save_settings({"cr_enabled": True})
    data = json.loads(config_file.read_text(encoding="utf-8"))
    assert data == {"cr_enabled": True}


def test_save_settings_creates_directory(tmp_path):
    config_dir = tmp_path / "subdir"
    config_file = config_dir / "settings.json"
    with patch.object(cfg, "_CONFIG_FILE", config_file), \
         patch.object(cfg, "_CONFIG_DIR", config_dir):
        cfg.save_settings({"cr_enabled": False})
    assert config_file.exists()
```

- [ ] **Step 2 : Vérifier que les tests échouent**

```powershell
cd "C:\Users\arnau\Documents\Projets perso\meeting-recorder"
D:\Utilitaires\Python\python.exe -m pytest tests/test_config.py -v
```

Résultat attendu : `ModuleNotFoundError: No module named 'config'`

- [ ] **Step 3 : Implémenter `src/config.py`**

Créer `src/config.py` :

```python
import json
from pathlib import Path

_CONFIG_DIR = Path.home() / ".meeting_recorder"
_CONFIG_FILE = _CONFIG_DIR / "settings.json"
_DEFAULTS: dict = {"cr_enabled": False}


def load_settings() -> dict:
    try:
        with open(_CONFIG_FILE, "r", encoding="utf-8") as f:
            data = json.load(f)
        return {**_DEFAULTS, **data}
    except (FileNotFoundError, json.JSONDecodeError):
        return dict(_DEFAULTS)


def save_settings(data: dict) -> None:
    try:
        _CONFIG_DIR.mkdir(parents=True, exist_ok=True)
        with open(_CONFIG_FILE, "w", encoding="utf-8") as f:
            json.dump(data, f, indent=2)
    except OSError as e:
        print(f"[config] Erreur écriture settings : {e}")
```

- [ ] **Step 4 : Vérifier que les tests passent**

```powershell
D:\Utilitaires\Python\python.exe -m pytest tests/test_config.py -v
```

Résultat attendu : 5 tests `PASSED`.

- [ ] **Step 5 : Vérifier que les tests existants n'ont pas régressé**

```powershell
D:\Utilitaires\Python\python.exe -m pytest tests/ -v
```

Résultat attendu : tous les tests `PASSED`.

- [ ] **Step 6 : Commit**

```powershell
git add src/config.py tests/test_config.py
git commit -m "feat: add config module for persistent settings"
```

---

## Task 2 : `tray.py` — toggle dans le menu

**Files:**
- Modify: `src/tray.py`

- [ ] **Step 1 : Ajouter l'import et la variable globale**

Dans `src/tray.py`, après `import time` (ligne 29), ajouter :

```python
import config
```

Dans le bloc des variables globales (après `_meeting_duration_s: float = 0.0`, ligne 37), ajouter :

```python
_cr_enabled: bool = False
```

- [ ] **Step 2 : Ajouter la fonction `_toggle_cr`**

Après la fonction `_show_progress_window` (après la ligne 216), ajouter :

```python
def _toggle_cr(icon, item):
    global _cr_enabled
    _cr_enabled = not _cr_enabled
    config.save_settings({"cr_enabled": _cr_enabled})
    print(f"[config] Génération CR : {'activée' if _cr_enabled else 'désactivée'}")
```

- [ ] **Step 3 : Mettre à jour `_build_menu`**

Remplacer la fonction `_build_menu` existante par :

```python
def _build_menu():
    return pystray.Menu(
        pystray.MenuItem(
            lambda _: (
                ("Arrêter et générer le CR" if _cr_enabled else "Arrêter l'enregistrement")
                if _recording else "Démarrer l'enregistrement"
            ),
            lambda icon, item: _stop(icon, item) if _recording else _start(icon, item),
            default=True,
            enabled=lambda _: not _processing,
        ),
        pystray.MenuItem(
            "Annuler le traitement",
            _cancel_processing,
            visible=lambda _: _processing,
        ),
        pystray.MenuItem(
            "Ouvrir la progression",
            _show_progress_window,
            visible=lambda _: _processing,
        ),
        pystray.Menu.SEPARATOR,
        pystray.MenuItem(
            "Générer le CR",
            _toggle_cr,
            checked=lambda _: _cr_enabled,
        ),
        pystray.Menu.SEPARATOR,
        pystray.MenuItem(
            "Ouvrir le dossier des CR",
            lambda icon, item: os.startfile(process.OUTPUT_DIR),
        ),
        pystray.Menu.SEPARATOR,
        pystray.MenuItem("Quitter", _quit),
    )
```

- [ ] **Step 4 : Initialiser `_cr_enabled` au démarrage**

Dans le bloc `if __name__ == "__main__":`, remplacer :

```python
    try:
        print("Démarrage Meeting Recorder...")
        _icon = pystray.Icon(
```

par :

```python
    try:
        print("Démarrage Meeting Recorder...")
        _cr_enabled = config.load_settings().get("cr_enabled", False)
        print(f"[config] Génération CR au démarrage : {'activée' if _cr_enabled else 'désactivée'}")
        _icon = pystray.Icon(
```

- [ ] **Step 5 : Commit**

```powershell
git add src/tray.py
git commit -m "feat: add CR generation toggle in tray menu"
```

---

## Task 3 : `tray.py` — `process_async` conditionnel

**Files:**
- Modify: `src/tray.py` (fonction `process_async` dans `_stop`)

- [ ] **Step 1 : Conditionner l'appel Gemini et les notifications**

Dans `process_async`, remplacer le bloc qui commence à `_set_state(icon, "generating", ...)` jusqu'à `os.startfile(process.OUTPUT_DIR)` :

Avant :
```python
            _set_state(icon, "generating", "Meeting Recorder — Génération du CR...")
            report = process.generate_report_from_text(transcription, on_progress=on_progress)
            md_path = process.save_report(report, timestamp)

            on_progress(process.ProgressEvent(step="done", pct=1.0, message=""))

            elapsed = int(time.monotonic() - _processing_start)
            duration_min = _meeting_duration_s / 60
            model_label = process._last_model_used or "Gemini"
            _set_state(icon, "idle", "Meeting Recorder")
            _notify(
                "Compte rendu prêt ✓",
                f"Réunion : {duration_min:.0f} min · Traitement : {elapsed} s · {model_label}"
            )
            os.startfile(process.OUTPUT_DIR)
```

Après :
```python
            if _cr_enabled:
                _set_state(icon, "generating", "Meeting Recorder — Génération du CR...")
                report = process.generate_report_from_text(transcription, on_progress=on_progress)
                process.save_report(report, timestamp)

            on_progress(process.ProgressEvent(step="done", pct=1.0, message=""))

            elapsed = int(time.monotonic() - _processing_start)
            duration_min = _meeting_duration_s / 60
            _set_state(icon, "idle", "Meeting Recorder")

            if _cr_enabled:
                model_label = process._last_model_used or "Gemini"
                _notify(
                    "Compte rendu prêt ✓",
                    f"Réunion : {duration_min:.0f} min · Traitement : {elapsed} s · {model_label}"
                )
                os.startfile(process.OUTPUT_DIR)
            else:
                _notify(
                    "Transcription prête ✓",
                    f"Réunion : {duration_min:.0f} min · Traitement : {elapsed} s"
                )
                os.startfile(process.meeting_folder(timestamp))
```

- [ ] **Step 2 : Vérifier que tous les tests passent toujours**

```powershell
D:\Utilitaires\Python\python.exe -m pytest tests/ -v
```

Résultat attendu : tous les tests `PASSED`.

- [ ] **Step 3 : Test manuel — CR désactivé (défaut)**

1. Lancer `pythonw src/tray.py`
2. Clic droit → vérifier que "Générer le CR" apparaît sans coche
3. Démarrer un enregistrement court → arrêter
4. Vérifier que la notification dit "Transcription prête ✓"
5. Vérifier qu'un `transcription.txt` est créé dans `~/Documents/GeneratedCR/CompteRendus/reunion_*/`
6. Vérifier qu'aucun `CR.md` n'est créé
7. Vérifier que le dossier de la réunion s'ouvre automatiquement

- [ ] **Step 4 : Test manuel — CR activé via toggle**

1. Clic droit → "Générer le CR" → doit afficher une coche
2. Relancer un enregistrement → arrêter
3. Vérifier que la notification dit "Compte rendu prêt ✓"
4. Vérifier qu'un `CR.md` est présent dans le dossier de la réunion
5. Quitter et relancer le tray → vérifier que la coche est toujours présente (persistance)

- [ ] **Step 5 : Commit**

```powershell
git add src/tray.py
git commit -m "feat: skip Gemini CR generation when toggle is off"
```
